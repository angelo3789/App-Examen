name: mi_app_gestion
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0' # Puedes tener una versión ligeramente diferente aquí

dependencies:
  flutter:
    sdk: flutter

  # Paquetes para Firebase
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0

  # Paquete para llamadas HTTP
  http: ^1.2.1

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true